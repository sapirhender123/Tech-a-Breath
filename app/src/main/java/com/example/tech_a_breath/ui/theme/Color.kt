package com.example.tech_a_breath.ui.theme

import androidx.compose.ui.graphics.Color

// ── Teal palette (sampled from app screenshot) ────────────────────────────────
val Teal50  = Color(0xFFD4EEF0)   // Splash bg, lightest fills
val Teal100 = Color(0xFFB0DDE2)   // Hover / pressed states
val Teal200 = Color(0xFF7FC4CC)   // Chip backgrounds
val Teal300 = Color(0xFF4DAAB5)   // Dividers, secondary icons
val Teal400 = Color(0xFF268A96)   // Secondary text accents
val Teal500 = Color(0xFF1A7A82)   // Subtitle color, mid accent
val Teal600 = Color(0xFF1A6E82)   // Cards / surfaces on dark bg
val Teal700 = Color(0xFF145C6E)   // Main UI background / primary dark
val Teal800 = Color(0xFF0F4D5D)   // Slightly deeper dark surface
val Teal900 = Color(0xFF0D4A52)   // Title text, deepest teal

val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)

// Calming Colors for Intervention - Richer, more "designed" palette
val DeepBackground = Color(0xFFF1F5F9) // Soft Slate 100
val SoftText = Color(0xFF334155) // Slate 700
val CalmingWave = Color(0xFF6366F1) // Indigo 500
val StopButton = Color(0xFF94A3B8) // Muted slate-blue
val AccentColor = Color(0xFFC7D2FE) // Indigo 200 for soft accents
val CardBackground = Color(0xFFFFFFFF).copy(alpha = 0.9f)

// ── Dashboard accent (used only in dashboard screens) ─────────────────────────
val Amber500 = Color(0xFFE9A825)
val Amber100 = Color(0xFFFFF0C2)

// ── Trigger indicator colors (dashboard SharedComponents) ─────────────────────
val TriggerSiren      = Color(0xFFEF5350)   // Red
val TriggerDogBark    = Color(0xFFFF8A65)   // Orange
val TriggerMotorcycle = Color(0xFFFFCA28)   // Amber
val TriggerFirework   = Color(0xFF29B6F6)   // Light blue
val TriggerUnknown    = Color(0xFF90A4AE)   // Blue-grey

val White = Color(0xFFFFFFFF)
val OnDark = Color(0xFFE2E8F0)
val OnDarkSub = Color(0xFF94A3B8)
